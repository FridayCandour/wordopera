/**
 components for the content editor

 title bar
 tools bar
 editing area
 side bar
*/


const title  = build("input",{type: "text", placeholder: "post title"})
const title  = build("input",{type: "text", placeholder: "post title"})
const title  = build("input",{type: "text", placeholder: "post title"})
const title  = build("input",{type: "text", placeholder: "post title"})
const title  = build("input",{type: "text", placeholder: "post title"})

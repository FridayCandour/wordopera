import build from "/node_modules/uiedbook";

/**
 components for the content editor
 title bar
 tools bar
 editing area
 side bar
*/


const title  = build("input",{type: "text", placeholder: "post title"})
const tools  = build("input",{type: "text", placeholder: "post title"})
const area  = build("input",{type: "text", placeholder: "post title"})
const side  = build("input",{type: "text", placeholder: "post title"})
const button  = build("input",{type: "text", placeholder: "post title"})
